import java.util.ArrayList;
import java.util.List;

public class Attacks {}
/*

    void surf(Pokemon name, Pokemon enemy);

    public void leafStorm(Pokemon name, Pokemon enemy);

    void hydroPump(Pokemon name, Pokemon enemy);

    void thunderPunch(Pokemon name, Pokemon enemy);

    void electroBall(Pokemon name, Pokemon enemy);

    void solarBeam(Pokemon name, Pokemon enemy);

    void flameThrower(Pokemon name, Pokemon enemy);

    void hydroCanon(Pokemon name, Pokemon enemy);

    void pyroBall(Pokemon name, Pokemon enemy);

    void thunder(Pokemon name, Pokemon enemy);

    void rainDance(Pokemon name, Pokemon enemy);

    void leechSeed(Pokemon name, Pokemon enemy);

    void leaveBlade(Pokemon name, Pokemon enemy);

    void inferno(Pokemon name, Pokemon enemy);

    void voltTackle(Pokemon name, Pokemon enemy);
}
*/
    /*

    public static List<Attack> getAttacks(String type)
    {
        List<Attack> allAttacks = new ArrayList<>();
        allAttacks.add(new Attack("Surf", 100, "Water"));
        allAttacks.add(new Attack("Fire Lash", 100, "Fire"));
        allAttacks.add(new Attack("Leafstorm", 100, "Grass"));
        allAttacks.add(new Attack("Hydropump", 100, "Water"));
        allAttacks.add(new Attack("Thunder Punch", 100, "Electric"));
        allAttacks.add(new Attack("Electro Ball", 100, "Electric"));

        List<Attack> attacksOfType = new ArrayList<>();

        if (type == null)
        {
            return allAttacks;
        }

        for (Attack attack : allAttacks)
        {
            if (attack.type.equals(type)) {
                attacksOfType.add(attack);
            }
        }

        return attacksOfType;
    }
*/
